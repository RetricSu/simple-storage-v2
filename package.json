{
  "name": "@polyjuice-provider/simple-storage-v2",
  "version": "0.0.1-rc6",
  "license": "MIT",
  "scripts": {
    "fmt": "prettier --write truffle-config.js migrations/* tests/* package.json",
    "lint": "eslint -c ../../.eslintrc.js \"src/**/*.ts\"",
    "build": "truffle compile",
    "test": "echo 'no test for simple-storage-v2'",
    "compile": "truffle compile",
    "clean": "rm -rf build/",
    "deploy": "truffle migrate --network development",
    "deploy:rinkeby": "truffle migrate --network rinkeby"
  },
  "dependencies": {
    "@polyjuice-provider/truffle": "0.0.1-rc6"
  },
  "devDependencies": {
    "prettier": "^2.3.2"
  }
}
